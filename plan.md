# Header.tsx changes:
1. Remove the "SEO Hub" completely. Integrations status shouldn't clutter the global header.
2. Simplify the logo animation.
3. Make the header slightly thinner (from h-20 to h-16).

# Sidebar.tsx changes:
1. Remove the duplicate logo at the top of the sidebar.
2. Flatten the active state of menu items (remove shadow and heavy gradients, use simple subtle background).
3. Ensure it starts right at the top since it's under the Header.

# MobileNav.tsx changes:
1. Ensure it's clean and has standard bottom navigation aesthetics (no heavy gradients).
